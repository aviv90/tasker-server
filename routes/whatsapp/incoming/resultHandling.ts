/**
 * Incoming Message Result Handling
 * 
 * Handles sending agent results (text, media, polls, locations) to WhatsApp
 * 
 * This file now acts as a facade, re-exporting from organized modules
 * for backward compatibility
 */

// Re-export everything from the organized modules
export * from './resultHandling/index';
